<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<title><%= transcription.title %> transcription</title>
<style type="text/css">
  body {
   color: black;
   background: white;    
   font-family: Georgia, Times, "Times New Roman", serif;
  }

  h1, h2, h3, h4, h5 {
    font-family: Helvetica, Arial, sans-serif;
    font-weight: normal;
  }

  h3, h4 {
    color:gray;
    font-size: 14px;
  }

  h3{
    margin-top: 8px;
    margin-bottom: 2px;
    line-height: auto;
  }

  h4{
    margin-top: 2px;
    margin-bottom: 2px;
  }

  p{
    margin-top: 2px;
    margin-bottom: 32px;
    line-height: 140%;
  }

  a[href], a[href]:link { 
    color: gray;
  }

  a[href]:visited {
    color: silver;
  }

</style>
</head>
<body>
<h1><%= transcription.title %> transcription</h1>
<h2><% if transcription.notes %><%= transcription.notes %><% end %></h2>
<% if transcription.url %><h3><a href="<%= transcription.url %>">Full audio</a></h3><% end %>

<% transcription.sort.each do |chunk| %>

<h3><%= chunk.offset_start %></h3>
<h3><audio src="<%= chunk.url %>" controls></audio></h3>
<h4><a href="https://requester.mturk.com/bulk/workers/<%= chunk.worker %>"><%= chunk.worker %></a></h4>

<p><%= chunk.body_as_html %>
<% end %>

</body>
</html>
